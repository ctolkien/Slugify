notifications:
  email: never
language: csharp
sudo: required
dist: trusty
mono: 4.0.5
dotnet: 1.0.0-preview2-003131
script:
- dotnet restore -v Minimal
- dotnet build **/project.json -c Release
- dotnet test tests/Slugify.Core.Tests
